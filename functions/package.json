{
  "name": "p2p-kyc-gate",
  "version": "2.0.0",
  "description": "Minimal Didit-backed identity verification gate for peer-to-peer crypto trades (Firebase Functions + Firestore)",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test",
    "serve": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions,hosting"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.1.0",
    "helmet": "^7.1.0"
  }
}
